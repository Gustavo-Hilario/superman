import React from "react";

// Components

import Button from "../Button";

const Home = (props) => {
    return (
        <div className="container-fluid">
            <h1 className="display-4 text-center fw-semibold">Home Page</h1>
            <Button title="Button Title Variable"></Button>
        </div>
    );
};

export default Home;
