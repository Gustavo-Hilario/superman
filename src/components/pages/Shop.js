import React from "react";

const Shop = (props) => {
    return (
        <div className="container-fluid">
            <h1 className="display-4 text-center fw-semibold">
                My React Shop!
            </h1>
        </div>
    );
};

export default Shop;
